# Flutter 4.4 Problem Library v0.1-alpha Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Flutter 客户端实现问题库 Top3 列表与详情页，对接 `GET /v1/problems` 与 `GET /v1/problems/{id}`，并与录音/结果页打通 `problemId` 透传，实现“一键开测→结果页→回看问题详情”的闭环。

**Architecture:** `ApiClient` 补 `getJson`（自动带 Authorization）；`ProblemsScreen` 拉 Top3 列表并展示 content_version；新增 `ProblemDetailScreen` 拉 bundle 并按 sections 渲染；`RecordScreen` 增加可选 `problemId` 参数并在结果页继续透传；`ResultScreen` 增加“查看问题详情”按钮（若有 problemId）。

**Tech Stack:** Flutter (Dart) + http（已封装 ApiClient）。

---

## 0. File Map

**Create:**
- `apps/mobile/lib/screens/problem_detail_screen.dart`

**Modify:**
- `apps/mobile/lib/api/api_client.dart`（新增 getJson）
- `apps/mobile/lib/screens/problems_screen.dart`（从占位变为列表）
- `apps/mobile/lib/screens/record_screen.dart`（新增 problemId + UI 展示 + 透传到 ResultScreen）
- `apps/mobile/lib/screens/result_screen.dart`（新增 problemId + “查看问题详情”按钮）

---

## Task 1: ApiClient 增加 getJson（自动带 Authorization）

**Files:**
- Modify: `apps/mobile/lib/api/api_client.dart`

- [ ] **Step 1: 新增 getJson**

```dart
Future<Map<String, dynamic>> getJson(
  String path, {
  Map<String, String>? headers,
}) async {
  final uri = Uri.parse('$baseUrl$path');
  final h = <String, String>{
    'Content-Type': 'application/json',
    ...?headers,
  };
  final token = await store.getToken();
  if (token != null && token.isNotEmpty) {
    h['Authorization'] = 'Bearer $token';
  }
  final res = await _client.get(uri, headers: h);
  if (res.statusCode < 200 || res.statusCode >= 300) {
    throw Exception('HTTP ${res.statusCode}: ${res.body}');
  }
  final decoded = jsonDecode(res.body);
  if (decoded is Map<String, dynamic>) return decoded;
  throw Exception('unexpected json shape');
}
```

- [ ] **Step 2: Commit**

```bash
git add apps/mobile/lib/api/api_client.dart
git commit -m "feat(mobile): add api getJson helper"
```

---

## Task 2: ProblemsScreen 实现 Top3 列表

**Files:**
- Modify: `apps/mobile/lib/screens/problems_screen.dart`

- [ ] **Step 1: 改为 StatefulWidget + loading/error/items**
- [ ] **Step 2: initState 拉取列表**

调用：
- `GET /v1/problems?limit=3`

渲染：
- 顶部显示：`content_version`
- 列表项：title + summary（缺省回退）
- 点击 push `ProblemDetailScreen(problemId: id, api: api)`

- [ ] **Step 3: 错误态与重试按钮**
- [ ] **Step 4: Commit**

```bash
git add apps/mobile/lib/screens/problems_screen.dart
git commit -m "feat(mobile): implement problems top3 list"
```

---

## Task 3: 新增 ProblemDetailScreen（拉 bundle 并渲染）

**Files:**
- Create: `apps/mobile/lib/screens/problem_detail_screen.dart`

- [ ] **Step 1: 页面入参**

```dart
class ProblemDetailScreen extends StatefulWidget {
  const ProblemDetailScreen({
    super.key,
    required this.api,
    required this.problemId,
  });
  final ApiClient api;
  final String problemId;
}
```

- [ ] **Step 2: 拉详情**

调用：
- `GET /v1/problems/{id}`

注意：响应为 bundleResp，结构为：
- `content_version`
- `problem`（object）
- `questions`（list）
- `suggestions`（list）
- `tools_guide`（object|null）

- [ ] **Step 3: 渲染 sections**

最小展示：
- problem：title/summary/tags（tags 可选）
- questions：逐条显示 text（+ type）
- suggestions：逐条显示 title（+ steps 前两条）
- tools_guide：默认折叠（ExpansionTile），展示 bullets/items（若存在）

- [ ] **Step 4: 底部 CTA：开始喵测**

push `RecordScreen(api: api, problemId: problemId)`

- [ ] **Step 5: Commit**

```bash
git add apps/mobile/lib/screens/problem_detail_screen.dart
git commit -m "feat(mobile): add problem detail screen"
```

---

## Task 4: RecordScreen 支持 problemId 透传

**Files:**
- Modify: `apps/mobile/lib/screens/record_screen.dart`

- [ ] **Step 1: Widget 入参增加 problemId**

```dart
const RecordScreen({super.key, required this.api, this.problemId});
final String? problemId;
```

- [ ] **Step 2: UI 上显示 problemId（可选）**

例如在状态卡里增加：
- `problem_id：...`

- [ ] **Step 3: 跳 ResultScreen 时透传**

```dart
ResultScreen(
  api: widget.api,
  audioUrl: _audioUrl!,
  audioId: _audioId,
  problemId: widget.problemId,
)
```

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/lib/screens/record_screen.dart
git commit -m "feat(mobile): pass problemId through record to result"
```

---

## Task 5: ResultScreen 增加“查看问题详情”

**Files:**
- Modify: `apps/mobile/lib/screens/result_screen.dart`

- [ ] **Step 1: 入参增加 problemId（nullable）**
- [ ] **Step 2: 成功态/降级态都可显示入口（按需）**

按钮：
- 若 `problemId != null`：
  - push `ProblemDetailScreen(api: widget.api, problemId: widget.problemId!)`

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/lib/screens/result_screen.dart
git commit -m "feat(mobile): add jump to problem detail from result"
```

---

## Task 6: Repo 校验 + nightly

- [ ] Run: `python3 scripts/ci/validate_repo.py`
- [ ] 观察 GitHub Actions 全绿
- [ ] nightly：`./scripts/package.sh /workspace/miaodong-nightly.zip`

